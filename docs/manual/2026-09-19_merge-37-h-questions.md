# 技術履歴: 教則第5版「ひっかけ問題」37問（2-H001〜2-H037）のマージ

最終更新: 2026-09-19

## 概要
`index.html` の `QUESTIONS` 配列の末尾に37問を追加（追加のみ・既存行の削除/変更なし）。問題プールは 78 → **115問**。追加分は二等のみ（`licenses:["second"]`）のため、二等 63 → **100問**、一等は 78 のまま。アプリのロジック・LocalStorage キー・UIには変更なし。

## スキーマ変換（外部データ → アプリ形式）
提供データは既存の `QUESTIONS` と項目名が異なるため、そのままは追加せず変換した（そのまま入れると選択肢・正解番号が読まれず出題が壊れる）。

| 提供データ | アプリの形式 | 備考 |
|---|---|---|
| `id: "2-H001"` | `id` | そのまま（既存は `ch1-01` 形式。衝突なし） |
| `class: "2"` | `licenses: ["second"]` | 二等用として追加 |
| `chapter: "CH2"` | `chapterID: 2` | 第2章に追加（章別ビューの件数は 二等 16→53） |
| `options` | `choices` | 4択 |
| `answer`（0始まり） | `correct` | 既存と同じ0始まり |
| （なし） | `category: "第5版 改正点"` | 必須項目のため付与（クイズ上部のタグに表示） |
| （なし） | `ref: "第2章 改正点（2026年7月14日適用）"` | 必須項目のため付与（解説の出典表記に表示） |

## 検証（マージ前・後）
- マージ前（`merge.js`）: 37件、ID連番（2-H001〜037）、既存IDとの重複なし、既存問題との本文重複なし、選択肢の重複なし、正解番号が範囲内、誤字パターン検出。
- マージ後: `<script>` 抽出で `node --check` OK。`QUESTIONS` 115件・ID全て一意・全件が同一スキーマ（違反0）。
- LocalStorage: 誤答リスト・進捗・試験日・`cbt_exam_history` を事前投入して読み込み、**読み込み後も内容が完全に同一**であることを確認（新規キーの作成もなし）。
- 機能回帰（モック）: CBT一連（出題50問は重複なし・新規問題が混入）、おすすめ10問、章別誤答消去（ゴールド演出）、設定メニュー/iCloudプレースホルダー、学習資料、報告フォーム。全て通過。
- 実ブラウザ（ヘッドレスChrome）: 起動して章別件数が `10/53/15/10/12`、進捗ゲージ「0 / 100問」を確認。追加問題（2-H020）の表示・採点・解説・出典表記も目視確認。

## データ修正（提供データからの変更点）
明らかな誤記のみ修正。内容が変わる修正は下記1件のみ。
- 誤字: 2-H023 解説「〜行うことが著しく、」→「〜行うことができ、」／2-H036・2-H037 の「許可手職等」→「許可手続等」（選択肢・解説）／2-H017 解説中の英語「of」→「の」。
- **2-H014（内容修正）**: 元の設問「『緩和』ではなく『強化』する方向に読み替えたものはどれか」は、正解に指定された選択肢（「農薬散布であれば一切の安全措置が不要となる」）が『強化』ではなく『過剰な緩和』であり、設問と正解が矛盾していた。設問文を「条件の確認を軽視し、過剰に緩和されたと読み替えている誤った理解はどれか」に改め、選択肢・正解・解説は元のまま。

## 要確認事項（未変更・出典確認を推奨）
- **2-H020**（満了日9/30の更新申請期間）: 正解は「3月31日〜8月31日」だが、暦どおりの「6か月前〜1か月前」は3月30日〜8月30日。月末日の数え方について国交省の取扱いを確認してから公開すること。
- **2-H003・2-H010**: 「敷地・区域の周囲おおむね1,000m」という前提の言い換え問題。測定起点（敷地境界か否か）の表現は公式資料に照らして確認を推奨。
- **2-H026/027/029**: 保険の要件（25kg以上、レベル3.5等）は「求められる」ことを前提にした記述で、公開解説に沿っているが原典（教則第5版）との突合は未実施。
- 出題内容全般は提供データを前提としており、教則第5版の原典との突合は未実施。
- 追加問題は二等のみ。一等試験にも同範囲が出題される場合は、`licenses` に `"first"` を追加する（一括置換で可）。
