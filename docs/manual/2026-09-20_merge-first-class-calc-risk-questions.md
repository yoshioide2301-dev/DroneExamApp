# 技術履歴: 一等「計算・リスク管理」問題 36問（1-C001〜1-C035, 1-C040）のマージ

最終更新: 2026-09-20

## 概要
`QUESTIONS` 配列の末尾に一等用（`licenses:["first"]`）36問を追加（追加のみ・既存行の削除/変更なし。2-H001〜050 は無変更）。問題プール 128 → **164問**（一等 78 → **114**、二等 113 のまま）。第3章の一等件数は 20 → 56。`CLAUDE.md` の総問題数は「全164問」に更新。ロジック・LocalStorage キー・UIは変更なし。

## 依頼内容との差異
- 依頼は「40問（1-C001〜1-C040）」だが、提供データは **1-C001〜1-C035 と 1-C040 の36問**で、**1-C036〜1-C039 の4問が欠落**していた。そのため総数は依頼の168ではなく **164**。4問が提供された時点で 168 になる（`merge3.js` の連番チェックは昇順のみを見るため、そのまま追記可能）。
- コミットメッセージは実態に合わせ「(1-C001 to 1-C035, 1-C040)」とした。

## データの補正
- 1-C021: 選択肢配列の途中に混入していたUI由来の文言「コードは注意してご使用ください。」を除去（そのままでは配列が構文的に壊れる）。
- 1-C018: 問題文中に混入していた韓国語の1文字「의」を「の」に修正。
- 1-C001: 解説「数値を当てします」→「数値を当てはめます」。
- `ref` の先頭「教則第5版 」を除去（例: `3.3.2項`）。UIが出典表記を「無人航空機の飛行の安全に関する教則（第5版） ＋ ref ＋ より引用・準拠」と組み立てるため、重複を避けた。
- それ以外の問題文・選択肢・正解・解説は提供データのまま。

## 検証
- マージ前（`merge3.js`）: ID形式・昇順・重複なし・既存問題との本文重複なし・選択肢重複なし・正解番号の範囲・混入文字（ハングル/UI文言/英単語）検出に加え、**計算問題30問（1-C001〜1-C030）を問題文の数値から再計算**し、正解選択肢が最も近く表示精度の丸め範囲内であることを確認（旋回半径 r=V²/(g·tanθ)、滑空距離＝高度×揚抗比±風補正、第一フレネル半径 R=17.3√(D/(4·f·1000))。不一致0）。
- マージ後: `node --check` OK。128→164件・ID全て一意・スキーマ違反0。
- LocalStorage: 一等/二等の誤答・進捗・試験日・`cbt_exam_history` を投入して読み込み、内容が完全に同一（新規キーなし）。
- 一等での動作（モック）: CBT50問（重複なし・1-C が混入・2-H は混入しない）、おすすめ10問、章別誤答消去（1-C001 の誤答登録→第3章の残数→0でゴールド演出、他章/二等の誤答は保持）。二等の出題に 1-C は混入しない。回帰: 二等CBT・設定メニュー・学習資料も通過。
- ヘッドレスChromeで計算問題（1-C021）の表示・採点・解説を目視確認。

## 要確認事項（未変更）
- **章分類**: 全問が `chapterID:3`（システム）。カテゴリーIII飛行のリスク管理（1-C031〜035, 040）は内容上は運航体制・リスク管理（第5章）寄りだが、提供データのまま第3章としている。章別「間違えた問題のみ」の件数はこの分類に従う。
- **出典の項番**（`3.1.2(2)4項`, `3.2.2項`, `3.3.2項`, `3.3.3項`）は提供データのまま。教則第5版の原典との突合は未実施。
- **1-C022・1-C024・1-C026・1-C028** の問題文「障害物の遮蔽が60%未満であるかを確認するための基準半径」は、第一フレネルゾーンの一般的な運用基準（60%以上の確保）の言い回しと整合しているか要確認（計算式・答えには影響しない）。
- **1-C016〜1-C020**（滑空距離への風の加減算）は「加減算量を固定値で与える」出題形式で、教則にこの計算手順が載っているか要確認。
- 解説に「現役講師の最短攻略」「パソコンの電卓では」等の口調があり、試験本番（CBT画面）では電卓の扱いが異なる可能性がある。
- 追加問題は一等のみ。二等にも出す場合は `licenses` に `"second"` を追加する。
