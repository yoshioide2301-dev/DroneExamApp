# 技術履歴: 一等「計算・リスク管理」40問（1-C041〜1-C080）のマージ

最終更新: 2026-09-20

## 概要
`QUESTIONS` 配列の末尾に一等用（`licenses:["first"]`）40問を追加（追加のみ・既存行の削除/変更なし）。問題プール 168 → **208問**（一等 118 → **158**、二等 113 のまま。第3章の一等件数は 60 → 100）。`CLAUDE.md` の総問題数は「全208問」に更新。ロジック・LocalStorage キー・UIは変更なし。

## 内容
- 1-C041〜050: 標高・相対高度・鉄塔/建築物の最上部との標高差（安全マージン）
- 1-C051〜060: バッテリー容量・平均電流・予備容量・風による電流増からの安全な最大飛行時間
- 1-C061〜070: カメラ画角と高度からの地上幅・1画素当たりの寸法、見込み角
- 1-C071〜080: カテゴリーIII飛行中の異常時対応（バッテリー異常発熱、GNSSロスト、異音、C2リンク不安定、補助者との連携、事後対応）

## データの補正（提供データからの変更点）
- **1-C061**: 選択肢配列の途中に混入していたUI由来の文言「コードは注意してご使用ください。」を除去（そのままでは配列が構文的に壊れる。前回の 1-C021 と同種）。
- **1-C043（内容修正）**: 正解番号が誤り。選択肢 `10m/20m/30m/40m` に対し正解が `1`（20m）だったが、解説・再計算とも 280m−250m＝**30m**（`2`）。
- **1-C046（内容修正）**: 正解番号が誤り。選択肢 `20m/30m/40m/50m` に対し正解が `2`（40m）だったが、解説・再計算とも 170m−140m＝**30m**（`1`）。
- **1-C060（解説のみ修正）**: 解説が「約28分」と述べていたが、選択肢に28分はなく正解は24分（`0`）。「28.8分を超えない最大の選択肢は24分」という説明に改めた（正解番号は変更なし）。
- それ以外の問題文・選択肢・正解・解説は提供データのまま。

## 検証
- マージ前（`merge5.js`）: ID連番（041〜080）、既存IDとの重複なし、既存問題との本文重複なし、選択肢重複なし、正解番号の範囲、混入文字検出、マージ後に 1-C001〜080 が全て存在すること。
- **計算問題24問を問題文から再計算**して正解を照合（標高差9問、バッテリー10問＝「計算値を超えない最大の選択肢」が正解であること、カメラ地上幅・画素寸法5問）。提供データのままの値で実行すると 1-C043・1-C046 が検出されることを確認済み（修正版は不一致0）。数値でなく式や文章を選ぶ 1-C047・1-C063 と、見込み角（1-C067〜070）、異常時対応（1-C071〜080）は再計算対象外（手計算で確認）。
- マージ後: `node --check` OK。208件・ID全て一意・スキーマ違反0。
- LocalStorage: 一等/二等の誤答・進捗・試験日・`cbt_exam_history` を投入して読み込み、内容が完全に同一（新規キーなし）。
- 動作（モック）: 一等CBT50問（重複なし、2-H は混入しない）、おすすめ10問、章別誤答消去（第3章 → 0件でゴールド演出、他章/二等の誤答は保持）、二等の出題に 1-C は混入しない。回帰（二等CBT・設定メニュー・学習資料）も通過。

## 要確認事項（未変更）
- **出題バランス**: 一等プール158問のうち 1-C 系は 76問（48%）、第3章が100問。一等のCBT50問では約半数（実測 23問）が計算・リスク管理問題になる。一等の模擬試験として章配分の偏りが問題なら、`buildCbtQuestions` に章配分を入れる改修を別途検討。
- **出典の項番**: 40問すべてが `3.3.4項`。標高差・バッテリー・カメラ・異常時対応と内容が多岐にわたるため、実際の教則の項番と一致しているか要確認（プレースホルダーの可能性）。
- **1-C051・054・057**（安全側に丸めた選択肢）: 計算値（25.2/28.8/33.6分）を下回る最大の選択肢が正解という設計。教則の解説方針に合うか要確認。
- **1-C072〜080**（異常時対応）は教則の記載に即した内容か原典との突合が未実施。1-C071/073/075/077/080 は「安全な着陸を優先」で論点が近い。
- 追加問題は一等のみ。二等にも出す場合は `licenses` に `"second"` を追加する。
