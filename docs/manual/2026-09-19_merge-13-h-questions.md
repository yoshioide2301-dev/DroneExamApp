# 技術履歴: 教則第5版「ひっかけ問題」残り13問（2-H038〜2-H050）のマージ

最終更新: 2026-09-19

## 概要
`QUESTIONS` 配列の末尾に13問を追加（追加のみ・既存行の削除/変更なし。前回の 2-H001〜037 は無変更）。問題プールは 115 → **128問**、二等は 100 → **113問**（第2章 53 → 66）、一等は 78 のまま。`CLAUDE.md` 内の総問題数も「全115問」→「全128問」へ更新。ロジック・LocalStorage キー・UIは変更なし。

## スキーマ変換
提供データは既存形式（`licenses`/`chapterID`/`choices`/`correct`）にほぼ一致。差分のみ補正した。
- `categ:"rules"`（`category` の誤記、かつ既存カテゴリ名と不一致）→ `category:"第5版 改正点"`（2-H001〜037と統一）。
- `ref` が無いため `"第2章 改正点（2026年7月14日適用）"` を付与（解説の出典表記に必須）。
- 誤記修正: 2-H048 選択肢「あるbut、」→「あるが、」／2-H047 解説「施行 of 改正」→「施行の改正」。その他の本文・選択肢・正解・解説は提供データのまま。

## 検証
- マージ前（`merge2.js`）: 13件・ID連番（2-H038〜050）・既存IDとの重複なし・既存問題との本文重複なし・選択肢の重複なし・正解番号が範囲内・英字の混入検出。
- マージ後: `node --check` OK。`QUESTIONS` 128件・ID全て一意・全件が同一スキーマ（違反0）。
- LocalStorage: 誤答・進捗・試験日・`cbt_exam_history` を投入して読み込み、読み込み後も内容が完全に同一（新規キー作成なし）。
- 機能回帰（モック）: CBT一連（50問は重複なし・新規問題が混入）、おすすめ10問、章別誤答消去・ゴールド演出、設定メニュー/iCloudプレースホルダー、学習資料。全て通過。
- 実ブラウザ（ヘッドレスChrome）: 章別件数 `10/66/15/10/12`、進捗ゲージ「0 / 113問」で起動を確認。

## 要確認事項（未変更）
- **法令の条文番号**（2-H039 都市計画法第8条第1項第1号、2-H040/041 航空法施行規則第236条の82第1項第2号）は提供データのまま。原典との突合は未実施。
- **2-H049**（満了日9/30 → 3/31〜8/31）は 2-H020 と同一シナリオ・同一の日付計算。暦どおりの「6か月前〜1か月前」は3/30〜8/30であり、月末日の数え方を国交省の取扱いで確認してから公開すること。
- **類似問題**: 2-H049≈2-H020、2-H042≈2-H019、2-H045≈2-H028、2-H038/035/036 など、同じ論点の言い換えが含まれる（本文は別のため重複判定はされない）。CBT/おすすめ10問では同論点が近接して出る可能性がある。
- 追加問題は二等のみ（`licenses:["second"]`）。一等にも出す場合は `"first"` を追加する。
